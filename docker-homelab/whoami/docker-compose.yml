services:
  whoami:
    image: traefik/whoami:latest
    container_name: whoami
    restart: unless-stopped
    networks:
      - whoami-net
    ports:
      - "${HOST_IP:-127.0.0.1}:8080:80" # Listens on port 8080 on the host

networks:
  whoami-net:
    driver: bridge
